# gitmsyncd-pg env — copy to /etc/gitmsyncd-pg/env on the host, fill real values.
# Password MUST match GITMSYNCD_DB_PASS in /etc/gitmsyncd/env.

POSTGRES_DB=gitmsyncd
POSTGRES_USER=gitmsyncd
POSTGRES_PASSWORD=<FILL_FROM_VAULT_same_as_GITMSYNCD_DB_PASS>
